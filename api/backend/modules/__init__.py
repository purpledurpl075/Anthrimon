# Optional, license-gated feature modules. Each module lives in
# modules/<name>/ with a manifest.yaml and a router. The loader mounts a
# module's router only when its license_key is covered by a valid license.
